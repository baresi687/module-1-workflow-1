// By default, the dev server (dev command) runs in development mode and the build command runs in production mode.

// console.log(process.env.VITE_API_KEY); // undefined
console.log(import.meta.env.VITE_API_USER) // 123
